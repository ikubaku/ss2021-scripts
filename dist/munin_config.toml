# The URL of the Library Index
library_index_url = "https://downloads.arduino.cc/libraries/library_index.json"

# The root directory for the database
database_root = "/mnt/work/database"

# Temporary directory for the analysis
temp_dir = "/tmp/munin"
